# Example

Input: "Use the latest Evidence about Learning Debt. Create LinkedIn, X Thread, and one
diagram. Fresh-review and revise it. Stop before publication."

Expected route: `task-intake-and-routing` → `evidence-refresh-and-bundle` →
`editorial-package` → `visual-storytelling`; no publication gate is immediate because the
request explicitly stops before publication.
