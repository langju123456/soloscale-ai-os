# Example

Input: two representative human-approved Run Receipts with stable steps, reusable checks,
and the same failure policy.

Expected route: propose a versioned Skill update with supporting Run IDs and compatibility
impact; do not mutate or promote the registered Skill automatically.
