# Example

Input: an approved seven-part X Thread and optional approved image.

Expected route: validate every part, show the exact thread, and stop at `PUBLISH`; a later
confirmed execution must preserve each post ID and stop on an ambiguous result.
