# Example

Input: configured Git, Conversation Knowledge, BuildLog, and application metadata scopes
plus the query "Learning Debt".

Expected route: refresh metadata without an LLM or network call, build one bounded
EvidenceBundle, preserve gaps and IDs, and write a private receipt without exposing bodies.
