# Example

Input: an approved final LinkedIn artifact, optional approved image, and exact account
reference.

Expected route: show the exact BuildLog preview and stop at `PUBLISH`; only a later confirmed
call may return a LinkedIn post ID and Publication Receipt.
